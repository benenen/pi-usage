{
  "name": "pi-usage",
  "version": "0.1.0",
  "description": "Per-provider plan usage status line for pi — OpenCode Go, ZAI/GLM Coding Plan, DeepSeek",
  "type": "module",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "preview": "bun run scripts/preview.ts"
  }
}
